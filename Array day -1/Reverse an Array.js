// Reverse an Array

// Write a function to reverse the elements of an array.

// Input: [1, 2, 3, 4, 5]

// Output: [5, 4, 3, 2, 1]

let arr = [1, 2, 3, 4, 5];

for (let i = arr.length; i >= 0; i--) {
  console.log(arr[i]);
}
